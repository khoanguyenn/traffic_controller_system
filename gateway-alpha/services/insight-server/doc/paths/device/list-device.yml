get:
  tags:
  - 'Device'
  summary: 'List devices'
  description: 'List devices'
  operationId: ListDevice
  consumes:
  - 'application/json'
  produces:
  - 'application/json'
  responses:
    200:
      description: 'OK'
      schema:
        $ref: '#/definitions/ListDeviceSuccess'
    500:
      description: 'Server Error'
      schema:
        $ref: '#/definitions/ListDeviceFail500'
